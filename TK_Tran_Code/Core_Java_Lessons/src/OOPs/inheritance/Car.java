package OOPs.inheritance;

public class Car extends Vehicle {        // Car inherits members and methods from Vehicle
    
    protected String model = "WRX";       // Car's member (protected, visible within package)
}