class Customer {

  constructor(customerId, firstName, lastName, birthday, usState) {
    this.customerId = customerId;
    this.firstName = firstName;
    this.lastName = lastName;
    this.birthday = birthday;
    this.usState = usState;
  }

}