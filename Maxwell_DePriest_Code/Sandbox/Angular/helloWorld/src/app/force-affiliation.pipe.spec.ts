import { ForceAffiliationPipe } from './force-affiliation.pipe';

describe('ForceAffiliationPipe', () => {
  it('create an instance', () => {
    const pipe = new ForceAffiliationPipe();
    expect(pipe).toBeTruthy();
  });
});
