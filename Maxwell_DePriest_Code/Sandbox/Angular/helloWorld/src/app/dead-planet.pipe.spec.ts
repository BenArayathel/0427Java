import { DeadPlanetPipe } from './dead-planet.pipe';

describe('DeadPlanetPipe', () => {
  it('create an instance', () => {
    const pipe = new DeadPlanetPipe();
    expect(pipe).toBeTruthy();
  });
});
